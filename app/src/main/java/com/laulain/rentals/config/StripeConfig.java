package com.laulain.rentals.config;

import org.springframework.context.annotation.Configuration;

/**
 * Stripe configuration — disabled for initial deployment.
 * Re-enable when Stripe integration is added.
 */
@Configuration
public class StripeConfig {
    // Stripe.apiKey initialization removed for initial deployment.
}
